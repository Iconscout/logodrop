const logo = (state = {}, action) => {
	switch(action.type) {
		default:
			return state
	}
}

const logos = (state = [], action) => {
	switch(action.type) {
		default:
			return state
	}
}

export default logos